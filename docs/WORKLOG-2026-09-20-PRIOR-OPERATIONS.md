# 2026-09-20 旧操作恢复与比较记录

## 为什么调整方向

用户指出近期操作退步。本次重新读取v2/v3/v4的原始完整归档，核对同一7069几何、失败集合和源码哈希，确认完成数从7068降至7065再降至7060。前次在较弱v4上叠加book联合过滤，不能代替回查此前更强操作。历史算法和前轮数据均保留。

## 完成的工作

1. 建立只读审计文档，保存全部14旧失败和最小图同状态首次分叉。ready硬筛选在该例推迟候选更紧的side13，转去给side4错误取2。
2. 新增旧九面十九共边引理的几何识别／验证与矩阵相交模块，额外检查输入矩阵包含全部十九条不等色前提。没有新增几何边，也没有颜色试探或失败后切换。
3. 实现固定三开关运行器。49图×8组合392次全部完成独立证明核验；147次plain基线精确复现原始取色和传播哈希。
4. 新组合对照显示恢复旧选面及初始化分别带来9→4→1的净冲突数改善，同时保留重新出现的对应旧失败；原v4加旧引理真实修复3例，剩6例。
5. 重用未改动的orbit和two-port精确入口，在同9图上18次均生成合法全图赋色。两者共享同一个精确内核。原编号外侧根BFS固定，每次30秒限制，无旧色输入、换序重试或失败回退。成功不算作修复原贪心语义。
6. 导出最小失败图的真实面约束图，实际通过原 `solve_closed_interfaces.py --mode orbit` 运行并保存完整结果。

## 可重复证据

- `outputs/prior-operations-factorial-2026-09-20.json.gz`：392次运行、全部证书及三旧基线核对，SHA `945cff0ccdc1e23ccc078232ab9c7c238e97757a73505a33078bdc3177095c15`。
- `outputs/prior-operations-factorial-summary-2026-09-20.json`：固定组合、逐版修复和退步、剩余图键与别名。
- `outputs/prior-exact-failures-2026-09-20.json`：18次精确程序原始见证、规模计数与单次描述时间，SHA `53241a464c78631b8d9b1cb0a5972217ffcaef6504f03747df25a9f55a706a30`。
- `examples/prior-v4-least-failure-graph.json` 及 `outputs/prior-orbit-example-2026-09-20.json`：最小20面53邻接图和旧CLI实际输出，峰值620边界状态。

诊断集含已知失败，不作为未知泛化证据；相邻历史前缀不是独立随机样本；不同方法的成功数没有拼接。全部时间不作性能基准，部分验证进程可能同时运行。

## 代码审查与验证

新增模块经过独立只读审查。补齐了十九条前提检查、完整模板缓存比对、累计双向删位计数、模式与初始化元数据校验，以及三旧基线的完整阶段哈希对照。旧模板312个合法赋值全部保留。

全项目 `python -m unittest discover -s tests -v`：674项通过。`python scripts/validate.py --output outputs/validation-prior-operations-2026-09-20.json` 再次运行674项也全部通过，并完成有限数学验证。最终核对前几轮和本轮七份报告中的源码哈希，全部一致；最小失败图旧CLI结果另按原始58条边逐条核验通过。未改JavaScript，不重复网页构建或Node全套测试。本轮没有提交、部署或上传GitHub。

## 后续基线

完整配色任务保留已有精确边界状态程序；一次性贪心研究回到历史最强v2，分析其唯一失败。ready作为几何信息可保留，但不默认升格为主动选面硬限制。新增组合需完整语料重跑才可替换已验证基线，不继续以版本更新次数代表进步。
