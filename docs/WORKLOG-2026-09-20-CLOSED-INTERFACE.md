# 2026-09-20：闭合接口实现与真实性能对照

用户接续“程序是否有优化、理论是否原创”的讨论，授权继续推进。范围固定为完整已知面邻接约束图上的精确求解优化，不将新增枚举隐蔽接入旧无回溯取名算法。

1. 先检查旧 `frontier_order.py`、内外顺序/最少调色板报告及已有接口理论，冻结旧输入和旧源文件。
2. 实现独立色名轨道压缩：完整内部见证同步作全色域置换，允许边界中未出现的颜色在已完成内部继续存在。专项穷举验证所有必要状态没有遗漏。
3. 实现约束图单割点分块。先叶后根求解各块，先根后叶对齐共享面颜色。迭代 Tarjan 避免递归深度限制；明确不将几何闭环自动等同割点块。
4. 新CLI及JSON例子可直接运行；显式区分三个模式，拒绝预指定颜色、列表等无法处理的附加约束。示例报告 `outputs/closed-interface-example-2026-09-20.json` 保留两三角块三色拼接结果。
5. 独立审计 `scripts/audit_closed_interfaces.py` 以另一种结构定义检查1,300图及全部装配接口，并以完整赋色检查4,412个可行/不可行案例。报告保存输入、见证、源码哈希，全部通过。
6. 完整单元测试601项通过后，单独执行冻结基准，避免与本任务CPU密集测试并行。正式报告保存11,502个确定性求解结果，3,834次原始计时及378次独立内存测量；源码、原始输入起止不变。
7. 保留重要负结果：最少两色的树不适合自动分块；块局部表更小却可能使用更多内存；色名压缩也有29个最少色计时条件更慢。推荐显式运行orbit，分块作为可选研究对照。
8. 基准结束后单独运行 `scripts/validate.py --output outputs/validation-closed-interface-2026-09-20.json`，601项测试和既有数学核验全部通过。最终实验、审计、示例的来源哈希与工作区一致。只修改研究代码与文档，未运行不相关的 Node/浏览器测试。

研究报告：[CLOSED_INTERFACE_OPTIMIZATION-2026-09-20.md](CLOSED_INTERFACE_OPTIMIZATION-2026-09-20.md)。理论方法已知；本轮报告项目内程序进展、正确性与有限性能证据。没有改变原v4、网页或历史实验，没有提交/上传本轮文件。旧本地图稿也继续保留。
