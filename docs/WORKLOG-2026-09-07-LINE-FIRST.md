# 2026-09-07 线优先命名工作记录

用户明确颜色是分割线名字的构件，不应从分块着色出发。因此停止把面分裂后的
Kempe 改色作为主实现，改为线名、方向、接点循环顺序及递归接枝模板。

本次新增：

- `fourcolor/line_names.py`：任意符号集上的线侧等号传播、反向语义、冲突路径、全局重命名。
- `tests/test_line_names.py`：22 项新测试，含 328 个完整与 747 个部分命名的独立核验。
- `scripts/validate_line_names.py` 与 `outputs/line-names-2026-09-07.json`：12,410 个有界输入通过。
- `docs/LINE_FIRST_RULES-2026-09-07.md`：精确定义、初等一致性证明、五符号反例、应用边界。
- `outputs/line-first-literature-2026-09-07.json`：定向检索记录，全文/书目范围与工具署名。
- `NEXT_STEP.md`：增补本次用户澄清，保留其他阶段的记录。

主线程执行完整 unittest discover 及 scripts/validate.py，当前工作区共 66 项通过，
其中含同时存在的 6 项树文法测试；不把这些树文法文件归为本次线命名实现。
完整报告另存 `outputs/validation-line-names-2026-09-07.json`，不覆盖原始验证报告。

本次没有实现缺失符号的四色选择，也没有宣称四符号上界由局部命名规则自动得到。
没有修改网页、发布远程内容、变更许可证或删除文件。原型是静态检查器，不冒称
已经实现每次编辑只更新局部的动态图算法。

检索发现组合地图、增益图、曲面边差和 HodgeRank 均有直接先例。潜在贡献应测试
特定递归操作的接口信息、更新成本和冲突证据；“无人研究”及可发表性尚未建立。
