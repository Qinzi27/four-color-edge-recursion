# 2026-09-20：继续推广闭合接口

用户要求澄清研究对象并继续推广。本轮把“从最内侧起步”推进为“什么接口关系足以保存局部可延拓性”，不将已有算法包装成原创突破。

1. 保留上一轮所有文件和证据；新增二元EQ/DIFF关系消元、残核显式求解和逆序恢复。范围是当前度≤2变量，未实现一般双分隔搜索。
2. 新增0至3有序接口的精确模式签名；同类收缩、异类补约束，保存一个完整见证。新辅助图不是地图新增几何边。
3. 实现显式双接口块拼接。两个块各自可行但同/异关系不一致时，正确报告交集为空，而不是强行对齐色名。
4. 将用户原图与旧已存几何生成的面邻接图对应，证明三接口联合约束“至多两色”，给出全部两两投影皆ALL却禁止012的反例；再推广到全部 `K2 ∨ kK1` 的闭式判据。
5. 文献核对 Dechter 的变量消元框架与已有NValue约束，明确理论基础重复；本轮的价值在具体映射、充分接口说明、边界反例和可复现实现。
6. 预声明83图186条件，引入独立DSATUR选点+精确回溯程序作更强参考。保留reduced相对它15快171慢的结果，所有速度限定为本机三轮、各公共API及其不同诊断开销。
7. 独立审计报告保存4140接口调用、8274模式查询、原图映射、族计数及320随机拼接。新CLI正例与不可行例均实际运行；输出拒绝覆盖。
8. 全部627单元测试通过，随后项目通用验证的627测试及既有数学核验也通过，保存 `outputs/validation-two-port-2026-09-20.json`。结束时复核七份新旧实验/示例报告，全部来源哈希保持一致。未修改网页，因此不重复Node或浏览器检查；本轮代码和报告仅本地。

主报告：[TWO_PORT_GENERALIZATION-2026-09-20.md](TWO_PORT_GENERALIZATION-2026-09-20.md)。后续要比较“从几何直接生成紧凑联合约束”能否优于逐模式查询，并验证与旧锚及既有Hall规则的相容性。
