# 2026-09-20：圈的 rank 与第二层可构造条件

用户给出一个外圈、两个内岛和三条连接线的手绘图，提出缺任意连接仍为两色、以及如何构造有区分度的最大/最小圈。沿用原始母线身份与当前两侧描述的区分，保留此前源码、原图与冻结结果。

本轮图意仍为条件假设：粉色和蓝色为真实边界，绿色为沿现有边的辅助圈；尚不把绿色是否为实际边当成用户已确认的事实。新图使用自明示意坐标，未改写截图像素或据截图推断精确几何测量。

## 完成内容

- `docs/CIRCLE_RANK-2026-09-20.md`：证明蓝线全部 8 种子集的两色/三色结论，分别定义包含深度、循环空间 rank、圈连接图 rank 与二进制层数；保留桥两侧同面的范围。
- 固定偶度 A 的第二层补全，归结为 `(V,A)` 上的 T-join：每个分量的剩余边出割数须为偶数。给出森林构造、关联矩阵 rank 条件和失败证书，并核验 Chekuri 原文 Proposition 3；不主张新的普遍定理。
- 给出双圈三径向弧的四色反例：它否定保留任意第一层总可补全，不否定地图四色性。提出固定 A 下最小/最大第二层总边权的 T-join 归约，明确尚未实现权重最优算法。
- `fourcolor/circle_layers.py`：独立标准库实现与证书检查，不导入旧取名求解器；地图调用方需先排除当前真实桥。`tests/test_circle_layers.py` 新增 11 项，含 76 个 n≤4 简单图的所有偶层及所有第二层独立穷举。
- `scripts/validate_circle_rank.py`：完整 8 子集示意几何，独立删边 BFS、面数、最少颜色和跨圈位编码核验；`outputs/circle-rank-2026-09-20.json` 记录全部几何、边类型、角色和源哈希。报告拒绝覆盖已验证。
- `scripts/render_circle_rank.py`：复用已有绘图 Canvas，从报告渲染 `docs/figures/circle-rank-2026-09-20/` 中 PNG、SVG 与 manifest。三组地图和两组圈层已人工视觉核查，颜色有数字/位编码冗余，源坐标比例保持一致。
- 说明包含英文研究摘要、原文链接与 Scientific Agent Skills 的工作流出处；工作流引用不作为数学证明证据。`NEXT_STEP.md` 追加本轮状态及真正剩余的项目问题。

## 验证

- `python -m unittest discover -s tests -v`：547 项通过，91.856 秒。
- `python scripts/validate.py --output outputs/validation-circle-rank-2026-09-20.json`：547 项通过，88.170 秒；四面体穷举、局部修复、递归构造及预着色障碍的项目综合检查通过。
- 全部 8 种蓝线子集通过，1–4 色共检查 3,778 个标记赋值；不是随机样本，也不是平面图全集验证。
- 三棱柱的五面邻接图：3 色合法赋值 0 个，4 色合法赋值 24 个。
- 图片输入/源码哈希、文本尾空白与 `git diff --check` 检查通过。PNG 为 1900×1370，SVG 保留可搜索文字。
- 没有修改旧网页、Node 模块或旧求解器；未重跑旧性能语料或将原型接入网页，不报告新性能优势。

本轮只添加本地研究材料，没有提交、推送或部署。原始提案与旧实验口径仍然保留。后续应在明确的线优先约束下定义 A 的选择、修复操作与改动代价，再证明图类范围和修复界。
