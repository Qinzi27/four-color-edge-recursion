# 2026-09-07：论文操作规则专项核验

阅读 CRZ 二叉树三符号文法的局部规则、共同词、同步分叉、非对称复制、三叶链扩展、子树拼接和置换去重；核看作者 PDF 第9页的两幅复制图。保留旧文档与原方法实现，新增一份独立对照笔记。

新增 `scripts/inspect_tree_grammar.py` 和6项语义测试，比较产生式解析器与独立子树区间 XOR oracle。完整核验1–7叶共197棵有序树、323,175个树词组合、9,806对同规模树，并对1,094个树对/叶位条件验证三种扩展。全部通过。

报告 `outputs/tree-grammar-rules-2026-09-07-02.json` 为完整版本；第一份报告只覆盖前两种扩展，予以保留。

数学结论：局部三面交界色差与三符号文法有精确对应。把 `((1,2),(3,4))` 直接转成二元非零群合并会丢失语义，不能用该转换失败否定原母线法。树对通用存在性不等于任意逐步选择都可延拓。

已在 `NEXT_STEP.md` 增补本轮入口。完整项目检查：`python -m unittest discover -s tests -v` 的44项测试通过；`scripts/validate.py` 通过，另存 `outputs/validation-tree-grammar-2026-09-07.json`；`git diff --check` 通过。

工作期间出现其他任务新增的 `fourcolor/line_names.py`，本轮未修改或纳入方法结论。未提交或推送本轮文件。
