# 2026-09-21：成本 4 的结构证书与可增长矩形族

用户要求“进行下一步测试”。沿前次 `KEMPE_PROGRESS-2026-09-20.md` 的下一步，先分析两个最少改 4 旧侧的实例，再检验相关约束变化和受限参数族。保留全部旧归档、旧结果和用户原创文字。

## 修改与原因

- 新增 `fourcolor/recoloring_boundary.py`：原变量上的精确 16 行子侧颜色成本表，支持固定侧、非负权重、完整最优支撑集和明确的搜索截断状态。
- 新增 `fourcolor/apex_triangle_cost.py`：固定通邻侧排除一种色后，用共享边三角形的等色证书压缩约束；逐类保存字面颜色成本，保持终点与净成本。
- 新增 `fourcolor/triangle_chain_family.py`：真实双行错缝矩形构造、完整贯穿切割历史和独立坐标验证。
- 新增 `scripts/validate_recoloring_obstructions.py`：冻结原例、抽象删边/删侧控制、包含极小核和真实几何族的联合验证；保存输入/源码 SHA-256。
- 四个新测试文件合计 27 项；覆盖独立全枚举、权重、截断、非法输入、几何篡改、证书缺行/重复行/错误计数/错误成本/错误见证以及完整 Kempe 分量。
- 新增 `scripts/render_recoloring_obstruction.py`、科研说明与本日志；追加 `NEXT_STEP.md`。

数学逻辑上的理由是：直接邻居只能给很弱的成本下界，约束传播会强制远处侧同色。原变量和等色商图两个求解方法互相核对；有外侧通邻条件才能使用三色推导。删边变体与真实几何实例分开报告。

## 本轮结论

- 20260926：24 个合法目标，8 个全局成本 4 的最优目标；六个可行子侧颜色对的最低成本均为 4。
- 20260980：12 个合法目标，唯一全局成本 4 的目标；六个可行颜色对最低成本为 `4,7,5,6,7,5`。
- 两例全部 52 个单边删除、17 个单旧侧删除及顺序删边核搜索，总计 244 个去重后的成本问题；由精确边界表和独立预算搜索交叉核验。
- 参数族 (m\ge1) 的最终内部面数为 (6m+3)，计费旧侧数 (6m+1)，最少净改色严格为 (2m)，一次完整 1/3 Kempe 交换即可达到。一般结论有直接证明；有限核验参数为 `1..8,16,32,64,100`。
- 最大正式实例 603 面、601 个计费旧侧，成本 200、Kempe 步数 1。不能把少量交换当作少量改色；小接口或小商图也不自动约束改色范围。

这些是指定合法初态的构造，不证明旧贪心流程会生成该初态，不宣称经过查新的原创算法，不改变已有 160 例或 7069 图成绩口径。

## 审查与验证

1. 独立数学审查核对了矩形族的几何共边、合法初态、三周期传播、六目标成本以及两个完整 Kempe 分量；另在内存核对 `m=1..10`。正式报告使用另列的固定参数。
2. 独立代码审查发现初版商图审计未保证 16 行覆盖、聚合结果与行见证一致；截断时最优计数字段也容易误读。已在首次正式报告前修复，加入笛卡尔积 oracle、篡改测试、未知计数字段置空和已见见证计数，并逐项核对家族证书。
3. 27 项新增专项测试通过。
4. `python -X utf8 -m unittest discover -s tests -v`：**749 项通过**，退出码 0，用时 135.887 秒。日志 `outputs/recoloring-obstructions-unittest-2026-09-21.txt`。
5. `python -X utf8 scripts/validate.py --output outputs/validation-recoloring-obstructions-2026-09-21.json`：退出码 0，综合数学验证通过；其内置测试再次运行 749 项通过。终端出现的拒绝覆盖提示来自原有拒绝覆盖测试，最终状态为 `OK`、`Validation report written.`。
6. `python -X utf8 scripts/validate_recoloring_obstructions.py --output outputs/recoloring-obstructions-2026-09-21.json`：`all_passed=true`。16 项输入/源码哈希开始结束一致；正式报告 SHA-256 为 `a1a10b9fab64565426744f022dfb6af7ea4d50eaa96726c2547bde44dd94598e`。
7. 绘图使用本机已有 Matplotlib 3.10.9 环境，未安装依赖或修改环境。两幅 PNG、对应 SVG、数据、图注和 manifest 保存至 `docs/figures/recoloring-obstructions-2026-09-21/`。两图均实际打开检查，标签、图例、斜纹、颜色数字与精确矩形布局清楚；输入和绘图脚本哈希可追溯。没有声称满足某个指定期刊规格。

研究核心与验证只需标准库。详细命令、一般证明及边界条件见 `RECOLORING_OBSTRUCTIONS-2026-09-21.md`。本轮保持本地研究材料，未提交、推送或部署。

## 下一步

固定本族几何及贯穿切割顺序，实际重放既有在线取名策略，检验其初态是否能进入刚性成本障碍。若不能，要找出并验证阻止障碍的策略不变量；另比较完整 Kempe 分量择优与更一般合法目标择优的旧侧成本。先定策略与成本，再汇报独立的新批次成绩。
