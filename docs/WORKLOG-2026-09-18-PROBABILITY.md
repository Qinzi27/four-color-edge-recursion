# 2026-09-18：概率与 AI 先例核对、有限概率实验

## 已完成

- 核对七项直接相关的原始文献；重点覆盖边界扩张计数、因子图求和、递归转移矩阵、有向 coedge 网络、结构 Transformer 和学习 BP。论文差异、检索词、访问限制与范围保存在 [研究说明](PROBABILITY_AI_REVIEW-2026-09-18.md)。
- 新增 `fourcolor/probabilistic_names.py`：三种可明确区分的统计模型，输入为旋转系统和 dart 似然；桥保持同侧；完整模型在 log 域精确枚举，不可行时明确报错。
- 新增 `tests/test_probabilistic_names.py`：10 项测试含独立面变量 oracle、桥、零权不可行、数值下溢、输入限制、整套 MAP、方向反转和符号置换。
- 新增 `scripts/validate_probabilistic_names.py`：种子 20260918，六个固定平面嵌入、三个噪声率、每组 12 次，共 216 次；原始报告保留全部实例，按来源文件 SHA-256 记录计算依据。
- 没有安装依赖，没有训练神经模型，没有改动原线名构造规则。已有改名、条带链和其他未提交工作原样保留；不提交或发布。

## 数值结果和边界

| 模型 | 隐藏符号准确率 | 隐藏符号平均 Brier | 整套 MAP 合法比例 |
| --- | ---: | ---: | ---: |
| independent | 0.2674933569530558 | 0.75 | 0.13425925925925927 |
| orbit | 0.7298494242692648 | 0.3351761170134551 | 0.6666666666666666 |
| joint | 0.829052258635961 | 0.2254913092717179 | 1.0 |

[概率报告](../outputs/probabilistic-names-2026-09-18.json)中 216 次独立 posterior oracle 检查全部通过，最大边缘概率误差约 8.55e-15。界面依赖反例也复现：桥两侧相等概率为 1，三角形某边两侧为 0，而只用各自边缘的乘积都给出 1/4。

这些差异验证了已知约束在指定合成任务上的作用。尚未检验新的神经架构、无损接口压缩、复杂递归历史、模型失配、真实图像、跨图泛化或相对已有先进算法的优势。六个拓扑上的重复观测不能视为广泛图族覆盖；没有把 MAP 的构造合法性当作经验定理。

## 执行检查

- 使用现有配套 Python 3.12.14，标准库；未使用 WindowsApps 启动占位符。
- `python -m unittest discover -s tests -v`：94 项通过。
- `python scripts/validate.py --output outputs/validation-probability-2026-09-18.json`：94 项通过，并完成原项目有限穷举检查。使用新输出文件保护旧报告。
- `python scripts/validate_probabilistic_names.py --output outputs/probabilistic-names-2026-09-18.json`：216 次通过。
- `git diff --check`：通过。新文件不包含私人机器路径或凭据。

首次集成运行暴露 `PlaneMap` 要求字符串顶点 ID，已在新增实验生成器入口显式转换；dart ID 仍是整数。该失败发生于写报告前，未覆盖任何结果。随后运行成功。

## 下一步

优先定义“相同可用信息、不同表示”的学习对照，至少保留普通因子图基线、有序线侧消息网络和结构 Transformer；先固定数据图族与训练预算，再决定是否安装训练依赖。先验证概率/错误解释任务确有实际价值，再扩展架构。若研究接口压缩，必须用不同外部拼接上下文检验联合信息损失。
